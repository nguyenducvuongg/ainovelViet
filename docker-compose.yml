services:
  ainovel:
    image: ghcr.io/nguyenducvuongg/ainovelViet:latest
    build:
      context: .
    volumes:
      - ./config:/root/.ainovel
      - ./workspace:/workspace
    stdin_open: true
    tty: true
